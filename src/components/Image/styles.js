import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  tinyLogo: {
    marginTop: 20,
    marginLeft: 20,
    width: '25%',
    height: '70%',
    borderRadius: 10,
  },
});
